# Copy this file to .env and fill in your database connection string.
# Prisma expects DATABASE_URL to be set when running `prisma generate` or other commands.
# Example Postgres connection string (replace placeholders):
# DATABASE_URL="postgresql://<DB_USER>:<DB_PASS>@localhost:5432/<DB_NAME>?schema=public"

# Example for development using a local Postgres:
# DATABASE_URL="postgresql://postgres:password@localhost:5432/apply_for_me?schema=public"

# If you prefer SQLite for local development, change `schema.prisma` datasource provider to `sqlite` and use:
# DATABASE_URL="file:./dev.db"

# After setting the DATABASE_URL, run:
#   npx prisma generate
# or
#   npx prisma migrate dev --name init

# ===========================
# Add your DATABASE_URL below
# ===========================
# DATABASE_URL=""
